// This is your Prisma schema file

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// Example model (you can delete or edit)
model User {
  id    Int    @id @default(autoincrement())
  name  String
  email String @unique
}
